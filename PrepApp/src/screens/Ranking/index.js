import React from 'react'
import { View, Text, Button } from 'react-native'

import { TopContent, Titulo } from './styles'

export default function Simulado ({ navigation }) {
  return (
    <TopContent>
      <Titulo>RANKING</Titulo>
    </TopContent>
  )
}
